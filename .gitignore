.env
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.DS_Store
*.db
*.sqlite
*.sqlite3
*.log

# Dependencies
node_modules/

# Browser Profiles (Private)
chrome_profile_notebooklm/
chrome_profile/

# Build Artifacts (Optional, but good practice to ignore dist if source is tracked)
# dist/
# (Keeping dist/ for now since we want the executable to be ready-to-run)

# Private embedded keys (contains actual API keys for distribution)
backend/config/embedded_keys_private.py

# User workspace (runtime data)
workspace/
